import { CesiumudsdkDirective } from './cesiumudsdk.directive';

describe('CesiumudsdkDirective', () => {
  it('should create an instance', () => {
    const directive = new CesiumudsdkDirective();
    expect(directive).toBeTruthy();
  });
});
